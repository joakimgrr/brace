import { h, Component } from 'preact';

require('./icon.scss')

export default class Icon extends Component {
    render() {
        return (
            <span class="ion-grid"></span>
        )
    }
}
